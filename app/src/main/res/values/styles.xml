<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="DefaultTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="DefaultTextStyleBlack" parent="@style/DefaultTextStyle">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="InputTextStyle" parent="@style/DefaultTextStyle">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding_small</item>
        <item name="android:textColorHint">@color/gray_light</item>
    </style>

    <style name="DefaultCard">
        <item name="android:layout_margin">@dimen/padding_default</item>
    </style>

</resources>
